const ROOT = 'Root' //Root tab 
const HOME = 'Home'
const ABOUT = 'About'
const LOGIN = 'Login'
const REGISTER = 'Register'

const NOT_FOUND = 'NotFound'

export {
    ROOT,
    HOME,
    ABOUT,
    LOGIN,
    REGISTER,
    NOT_FOUND
}