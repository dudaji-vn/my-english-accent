import {ScrollView} from 'native-base';
import React from 'react';
import {Text} from 'react-native';

type Props = {};

const WordsRecordScreen = (props: Props) => {
  return (
    <ScrollView>
      <Text>WordsRecordScreen</Text>
    </ScrollView>
  );
};

export default WordsRecordScreen;
